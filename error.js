

var the_text = "abcd1234" + "cdefghi3393"
// this is an object declaration
var never_reassigned = {}
never_reassigned.name = "ebube okorie..."
the_text = 12223;
/* 

this is a very
long
comment.
 */




var toBeReassigned = {};
toBeReassigned = { name: "ana" };
toBeReassigned.name = 1;
toBeReassigned = 0;
toBeReassigned = { name: "ana" };


/*
This is another multi-line comment.
It can span multiple lines.
*/
let result = text
    .split(",")
    .map((letter) => {
        return letter.toUpperCase();
    })
    .join(".");
console.log(result);
